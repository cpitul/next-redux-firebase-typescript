// URLS
export enum URLS {
	// WEBSITE
	HOME = '/',
	LOGIN = '/login',
	REGISTER = '/register',

	// API
}

export const DEFAULTS = {};

export enum LS {
	LANG = 'wa-lang',
	IS_LOGGED = 'wa-is-logged',
}

export enum StateStatus {
	idle = 'idle',
	loading = 'loading',
	failed = 'failed',
	fulfilled = 'fulfilled',
}

export enum Langs {
	en = 'en-US',
}

export enum AccountType {
	standard = 'standard',
}

// HEAD
export enum TITLE {
	HOME = 'Home',
}

export enum DESCRIPTION {
	HOME = 'Writer Assist',
}

export enum OG_DESCRIPTION {
	HOME = 'Writer Assist',
}

export enum OG_TITLE {
	HOME = 'Writer Assist',
}

export enum KEYWORDS {
	HOME = 'writer assist',
}
